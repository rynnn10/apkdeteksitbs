{
  "name": "tbs-deteksi-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "android:init": "npx cap init TBS_Deteksi com.tbs.deteksi --web-dir=dist",
    "android:add": "npx cap add android",
    "android:sync": "npx cap sync",
    "android:open": "npx cap open android",
    "android:build": "npm run build && npx cap sync && npx cap open android"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^4.21.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.0",
    "recharts": "^2.12.7"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
